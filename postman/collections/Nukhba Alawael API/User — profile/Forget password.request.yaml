$kind: http-request
url: "{{baseUrl}}/api/user/mine/forget-password"
method: POST
headers:
  Content-Type: application/json
body:
  type: json
  content: |-
    {
      "email": "user@example.com"
    }
auth:
  type: noauth
order: 5000
