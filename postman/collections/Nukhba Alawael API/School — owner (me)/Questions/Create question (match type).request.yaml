$kind: http-request
description: "matchingItems types: base (needs correctIndex, index into the match rows) | match. Minimum 3 items."
url: "{{baseUrl}}/api/school/me/questions"
method: POST
headers:
  Content-Type: application/json
body:
  type: json
  content: |-
    {
      "title": "Match the pairs",
      "type": "match",
      "purpose": "lesson",
      "lessonId": "00000000-0000-0000-0000-000000000000",
      "matchingItems": [
        { "text": "Cat", "type": "base", "correctIndex": 0 },
        { "text": "Meow", "type": "match" },
        { "text": "Bark", "type": "match" }
      ]
    }
order: 4000
