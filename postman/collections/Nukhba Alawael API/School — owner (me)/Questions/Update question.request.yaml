$kind: http-request
description: To attach an image switch body to form-data with an `image` file field.
url: "{{baseUrl}}/api/school/me/questions/:id"
method: PATCH
headers:
  Content-Type: application/json
pathVariables:
  id: ""
body:
  type: json
  content: |-
    {
      "title": "Updated title",
      "type": "options",
      "options": [
        { "text": "4", "isCorrect": true },
        { "text": "5", "isCorrect": false }
      ]
    }
order: 6000
